<Project>

	<PropertyGroup>
		<GeneratePackageOnBuild>false</GeneratePackageOnBuild>
		<Product>Voyager.DBConnection</Product>
		<Description>Modern, testable database access library using DbProviderFactory with Result monad pattern for explicit error handling. Features IDbCommandExecutor interface for easy mocking, command factory pattern for separation of concerns, and fluent parameter API. Supports SQL Server, PostgreSQL, MySQL, Oracle, SQLite, and ODBC for universal database connectivity.</Description>
		<RepositoryType>git</RepositoryType>

		<Title>Voyager.DBConnection - Testable Database Access with Result Pattern</Title>

		<PackageLicenseExpression>MIT</PackageLicenseExpression>
		<RepositoryUrl>https://github.com/Voyager-Poland/Voyager.DBConnection.git</RepositoryUrl>
		<PackageProjectUrl>https://github.com/Voyager-Poland/Voyager.DBConnection</PackageProjectUrl>
		<Authors>Andrzej Świstowski</Authors>
		<PackageTags>database;ado.net;dbproviderfactory;result-monad;command-pattern;testable;mocking;sqlserver;postgresql;mysql;oracle;sqlite;odbc;error-handling;functional-programming</PackageTags>
	</PropertyGroup>

</Project>